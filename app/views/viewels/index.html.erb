
<% @viewels.each do |viewel| %>

  <div class="thumbnails">
   <div class="col-md-4">
   	<%= link_to(image_tag('http://placehold.it/200x200'), viewel.body, viewel, class: 'thumbnails') %>
   </div>
</div>

<% end %>